jar {
    manifest {
        attributes 'Plugin-Class': 'GUI'
    }
}

dependencies {
    compile project(":QuestionTypes:QuestionType");
    compile project(":QuestionTypes:ShortAnswer");
    compile project(":QuestionTypes:MultiChoice");
    
    compile project(":PluginCore");
}
